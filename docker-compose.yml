version: '3.8'

services:
  budgetwise-ui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_SUPABASE_URL=https://ygftwndqficrfwajslaj.supabase.co
        - VITE_SUPABASE_ANON_KEY=sb_publishable_fNbGtXnG5iGhHaKaiV_vbQ_WuqP1JOr
    container_name: budgetwise-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
